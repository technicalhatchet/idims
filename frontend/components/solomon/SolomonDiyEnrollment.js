/**
 * DIY enrollment now runs inside useSolomonAuth (callback + client retry).
 * This component is kept as a no-op mount point for backwards compatibility.
 */
export default function SolomonDiyEnrollment() {
  return null;
}
